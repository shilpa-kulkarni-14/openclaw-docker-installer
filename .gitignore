# Environment files (contain API keys — never commit)
.env
.env.local
.env.*.local

# Docker secrets
secrets/

# OS files
.DS_Store
Thumbs.db

# Logs
*.log

# Build timestamp (used to detect when to force rebuild)
.last-build
